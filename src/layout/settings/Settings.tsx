import SettingsIcon from '@/assets/icons/settings-icon.svg'

import * as SC from './SettingsStyles'

const Settings = () => {
	return (
		<SC.SettigsWrapper>
			<SC.SettingsLogo src={SettingsIcon} />
		</SC.SettigsWrapper>
	)
}

export default Settings
